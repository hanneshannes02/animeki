{
  "name": "animeki",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "snapshot": "node scripts/generate-snapshot.mjs",
    "build": "vite build",
    "test": "echo \"No tests configured\""
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
